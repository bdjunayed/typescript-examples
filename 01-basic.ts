var item: string = 'chicken';
var calories: number = 300;
var tasty: boolean = true;

function details(menu: string, energy: number) {
    console.log("We go "+energy+" for the "+menu);
}

details(item, calories);